using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobotSpawn : MonoBehaviour
{
    public GameObject theRobot;

    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(theRobot, new Vector3(clusterPositionX, 0, cluterPositionZ), Quaternion.identity);
    }
            
}
